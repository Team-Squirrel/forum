import * as utils from "utils";

class User {

    constructor(name, email, country) {
        this.name = name;
        this.email = email;
        this.role = "user";
        this.createdOn = utils.date.getDate();
        this.id = utils
    }

}